<UserControl x:Class="Oqat.ViewModel.VM_ProjectExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Oqat.Model"
    xmlns:WinApiPresentation="clr-namespace:Microsoft.WindowsAPICodePack.Controls.WindowsPresentationFoundation;assembly=Microsoft.WindowsAPICodePack.Shell"
    xmlns:WinApiShell="clr-namespace:Microsoft.WindowsAPICodePack.Shell;assembly=Microsoft.WindowsAPICodePack.Shell"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
    <TabControl Grid.Row="0" Grid.RowSpan="4">
        <TabItem >
            <TreeView  x:Name="smartTreeExplorer" ItemsSource="{Binding}"
                      SelectedItemChanged="onSmartNodeSelect">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:SmartNode}"
                                              ItemsSource="{Binding smartTree}">
                        <TextBlock Text="{Binding Path=name}"/>
                    </HierarchicalDataTemplate>                  
                </TreeView.Resources>
            </TreeView>
        </TabItem>
        <TabItem>

                <!--FileBrowser-->
                <Border Width="400">
                    <WinApiPresentation:ExplorerBrowser                        
                        x:Name="fileExp"
                        Margin="5"
                        NavigationTarget="{Binding SelectedItem, ElementName=KnownFoldersList, Mode=OneWay}" />
                </Border>
            </TabItem>
    </TabControl>
        <StackPanel Grid.Row="5">
            <StackPanel.DataContext>
                <Binding ElementName="smartTreeExplorer"  Path="SelectedItem"/>
            </StackPanel.DataContext>
            <WrapPanel>
                <Label Content="Title:"/>
                <Label  Content="{Binding name}"/>
            </WrapPanel>
            <WrapPanel>
                <Label Content="Path:"/>
                <Label Content="{Binding video.vidPath}"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
