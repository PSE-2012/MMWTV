<UserControl x:Class="Oqat.ViewModel.Macro.MacroMetricControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Row="0" Grid.Column="0" Name="ScrollViewer1" VerticalAlignment="Top" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Visible">
            <Grid>
                <StackPanel>
                    <ListView Name ="macroTable" ItemsSource="{Binding macroTable}" HorizontalAlignment="Stretch" FontSize="13" AllowDrop="True" SelectionMode="Extended">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn x:Name="hd5" Header="Metric Name" DisplayMemberBinding="{Binding pluginName}"/>
                                <GridViewColumn x:Name="hd2" Header="Memento Name" DisplayMemberBinding="{Binding mementoName}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Grid>
        </ScrollViewer>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="deletebutton" Content="Gewählten Eintrag löschen" Click="Delete_Click"   HorizontalAlignment="Stretch" VerticalAlignment="Bottom" ></Button>
        </Grid>
        <Grid Grid.Row="2">
            <StatusBar Name="sbar" 
           VerticalAlignment="Bottom" Height="32">

                <StatusBarItem>
                    <TextBlock x:Name="tb1">Verarbeite Video</TextBlock>
                </StatusBarItem>
                <StatusBarItem>
                    <ProgressBar Width="170" Height="20"
                 Name="progressBar">

                    </ProgressBar>
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Name="percent" Width="50"></TextBlock>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</UserControl>
