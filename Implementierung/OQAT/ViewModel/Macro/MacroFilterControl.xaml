<UserControl x:Class="Oqat.ViewModel.Macro.MacroFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Row="0" Grid.Column="0" Name="ScrollViewer1" VerticalAlignment="Top" ScrollChanged="scroll" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
            <Grid>
                <StackPanel>
                    <ListView Name="macroTable" ItemsSource="{Binding macroQueue}" HorizontalAlignment="Stretch" FontSize="13" AllowDrop="True" SelectionMode="Extended">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn  Header="Plugin Name" DisplayMemberBinding="{Binding pluginName}"/>
                                <GridViewColumn  Header="Memento Name" DisplayMemberBinding="{Binding mementoName}"/>
                                <GridViewColumn  Header="Start Frame" DisplayMemberBinding="{Binding startFrameRelative}"/>
                                <GridViewColumn  Header="End Frame" DisplayMemberBinding="{Binding endFrameRelative}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Grid>
        </ScrollViewer>
        <ScrollViewer Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" Name="ScrollViewer2" ScrollChanged="scroll2" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Visible">
            <Grid>
                <ListView HorizontalAlignment="Stretch" Name="rangeSliders">
                </ListView>
            </Grid>
        </ScrollViewer>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Name="deletebutton" Content="Delete Selected Entries" Click="Delete_Click"   HorizontalAlignment="Stretch" VerticalAlignment="Bottom" ></Button>
        </Grid>
    </Grid>
</UserControl>
