<UserControl x:Class="Oqat.ViewModel.Macro.MacroFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" Name="ScrollViewer1"  VerticalAlignment="Top" ScrollChanged="scroll" HorizontalAlignment="Left" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
            <Grid>
                <ListView HorizontalAlignment="Left" FontSize="13" Name="macroTable" AllowDrop="True" SelectionMode="Extended">
                </ListView>
            </Grid>
        </ScrollViewer>
        <ScrollViewer Grid.Row="1"  VerticalAlignment="Top" Name="ScrollViewer2" ScrollChanged="scroll2" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Visible" >
            <Grid>
                <ListView HorizontalAlignment="Left" Name="rangeSliders">
            </ListView>
            </Grid>
        </ScrollViewer>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Name="savebutton" Content="Save Macrofilter" Click="Save_Click"   HorizontalAlignment="Center" VerticalAlignment="Bottom" ></Button>
            <Button Grid.Column="2" Name="deletebutton" Content="Delete Selected Entries" Click="Delete_Click"   HorizontalAlignment="Center" VerticalAlignment="Bottom" ></Button>
        </Grid>
    </Grid>
</UserControl>
