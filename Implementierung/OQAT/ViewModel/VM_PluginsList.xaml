<UserControl x:Class="Oqat.ViewModel.VM_PluginsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:local="clr-namespace:Oqat.ViewModel">
    <StackPanel Orientation="Vertical">
        <TabControl SelectionChanged="tab_SelectedChanged">
            <TabItem Header="Filter" Name="tabFilterList">
                <TreeView Name="treeFilters" ItemsSource="{Binding}" SelectedItemChanged="treePlugins_SelectedItemChanged">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type local:PluginViewModel}"
                                              ItemsSource="{Binding children}">
                            <TextBlock Text="{Binding Path=name}"/>
                        </HierarchicalDataTemplate>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsSelected" Value="{Binding Path=selected, Mode=TwoWay}" />
                        </Style>
                    </TreeView.Resources>
                </TreeView>
            </TabItem>
            <TabItem Header="Metriken" Name="tabMetricList">
                <TreeView Name="treeMetrics" ItemsSource="{Binding}" SelectedItemChanged="treePlugins_SelectedItemChanged">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type local:PluginViewModel}"
                                              ItemsSource="{Binding children}">
                            <TextBlock Text="{Binding Path=name}"/>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
            </TabItem>
        </TabControl>
        
        <StackPanel>
            <StackPanel Orientation="Vertical" Name="panelMementoSave" Visibility="Hidden">
                <TextBox Name="tbMementoName" Margin="3">Filtereinstell1</TextBox>
                <StackPanel Orientation="Horizontal">
                    <Button Name="bttSaveMemento" Margin="3" Click="bttSaveMemento_Click">Speichern</Button>
                    <Button Name="bttCopyMemento" Margin="3" Click="bttCopyMemento_Click">Kopieren</Button>
                    <Button Name="bttDeleteMemento" Margin="3" Click="bttDeleteMemento_Click">Löschen</Button>
                </StackPanel>
            </StackPanel>
            <ScrollViewer Name="gridPluginProperties" />
            <Button Name="bttAddToMacro" Click="bttAddToMacro_Click">hinzufügen</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
